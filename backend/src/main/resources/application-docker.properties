# Docker profile: datasource and OPENAI_API_KEY come from docker-compose env.
# Keeps backend from using application.properties local DB settings.
# Key optional so container starts; AI endpoints return clear error if missing.
openai.api.key=${OPENAI_API_KEY:}
